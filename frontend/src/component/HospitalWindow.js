import React from 'react';
function HospitalWindow({match}) {
    
    // const product = data.product.find((x) => Number(x._id)  === Number(props.match.params.id));
    // const prod = data.hospitals.find(x=>x.properties._id===props.match.params.id);
    // let params = match.params;
    // console.log(params);
    return (
        <div>
            {/* {params} */}
        </div>
    );
}

export default HospitalWindow;